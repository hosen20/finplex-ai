# Finplex AI Dashboard

Professional React dashboard for Finplex AI.

## Run locally

```bash
cd apps/web
cp .env.example .env
npm install
npm run dev
```

Open http://localhost:5173.

The dashboard expects the FastAPI backend at `http://localhost:8000` by default.

# PR 1: Local Product Cleanup

This patch makes the repository local-only and removes generated files that should not be committed.
It also removes Capacitor dependencies from the React web app because the final product scope is web + Streamlit admin only.

Run the commands from the assistant response after extracting this zip.
